---
interface Props {
  text: string;
  styles?: string;
}

const { text, styles = '' } = Astro.props;
---

<h2 class={`leading-[150%] tracking-[-1.1%] text-white md:text-xl lg:text-2xl ${styles}`}>
  {text}
</h2>
